<%= form_with(model: artist, local: true) do |form| %>
  <% if artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  </div>

  <div class="field">
  <%= form.label :started %>
  <%= form.text_field :started %>
  </div>
  </div>

  <div class="field">
    <% if Album.all != [] %>
    <%= select("album", "id", Album.all.collect {|p| [ p.title, p.id ]}) %>
    <% end %>

  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
